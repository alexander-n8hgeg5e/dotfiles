bind   index                     /               limit
bind   generic                   <Delete>        noop
bind   index                     <Delete>        delete-message
bind   pager                     <Delete>        noop
bind   index                     k               half-down
bind   generic                   k               previous-entry
bind   pager                     k               half-down
macro  index                     <F2>            "<shell-escape>mutt-notmuch --prompt search<enter><change-folder-readonly>~/.cache/mutt_results<enter>"    "search mail (using notmuch)"
bind   compose                   <Right>         view-attach
#macro  index                     <Left>          "c?\t"
#macro  index                     <Left>          "c?\t" "Go back to the list of mailboxes"
bind   pager                     <Left>          exit
bind   index                     <Return>        display-message
bind   index                     <Return>        display-message # TODO: This might not be needed
bind   index                     <Right>         display-message
bind   pager                     <Right>         view-attachments
bind   index                     <Space>         tag-entry
bind   index                     <Tab>           next-new-then-unread
bind   index                     i               half-up
bind   pager                     i               half-up
bind   generic                   i               previous-entry
bind   index                     @               display-address
bind   pager                     @               display-address
bind   index                     A               reply
bind   pager                     A               reply
bind   index                     a               noop
bind   pager                     a               noop
bind   index                     B               bounce-message
bind   pager                     B               bounce-message
bind   index                     c               change-folder
bind   index                     C               copy-message
bind   pager                     C               copy-message
bind   index                     D               noop
bind   compose                   D               edit-description
bind   generic                   d               noop
macro  index                     d               "jjj"
bind   pager                     d               noop
bind   generic,index             <Down>          next-entry
bind   pager                     <Down>          next-line
bind   generic,index             <Up>            previous-entry
bind   pager                     <Up>            previous-line
bind   index                     E               edit
bind   pager                     E               edit
bind   generic                   e               noop
macro  index                     e               "kkk"
bind   pager                     e               noop
bind   index                     f               display-message
bind   compose                   f               view-attach
bind   index                     F               forward-message
bind   pager                     F               forward-message
macro  index                     g               "<change-folder>?" "goto folder..."
macro  index                     h               "<pipe-message>bogofilter -n<enter>"  "teach bogofilter non spam"
#macro  index                     J               "<tag-prefix><pipe-message>bogofilter -s<enter><tag-prefix><save-message>=INBOX.junk<enter><tag-prefix><delete-message>"  "Move to junk"
macro  index                     J               "<pipe-message>bogofilter -s<enter><save-message>=INBOX.junk<enter><delete-message>"  "Move to junk"
bind   pager                     j               noop
#bind   generic,index             j               next-entry
bind   index                     L               edit-label
bind   pager                     l               noop
#bind   index                     n               mail
#bind   pager                     n               mail
bind   index                     O               recall-message
bind   pager                     O               recall-message
bind   index                     o               resend-message
bind   pager                     o               resend-message 
bind   compose                   p               noop
bind   compose                   P               postpone-message
bind   pager                     p               noop
bind   pager                     P               noop
bind   index                     p               sort-mailbox
bind   index                     q               quit
#macro  index                     R               "<tag-prefix><clear-flag>N"
macro  index                     R               "<clear-flag>N"
macro  index                     r               '<shell-escape>mailstart<enter><sync-mailbox><change-folder>^<enter>' "Reload mailbox"
bind   index                     S               save-message
bind   compose                   s               exit
bind   compose                   S               edit-subject               
bind   index                     s               noop
bind   index                     U               undelete-message
#bind   index                     V               toggle-read
bind   index                     W               clear-flag
bind   index                     w               set-flag
bind   compose                   y               noop
bind   pager                     y               noop
bind   editor                    y               noop
bind   compose                   Y               send-message
bind   compose                   <Esc>D          detach-file
bind   attach                    D               delete-entry
macro  index                     Z               "<shell-escape>runbogomailfilter<enter>" "run the runbogomailfilter script"
bind   index                     |               pipe-message
bind   pager                     |               pipe-message
bind   attach                    <Left>          exit
bind   attach                    <Right>         view-attach
bind   generic                   <Delete>        noop
macro  browser                   <Left>          "qc?\t" "goto folder..."
bind   generic                   <Return>        select-entry
bind   generic                   <Right>         select-entry
macro  browser                   g               qc? "goto folder..."
bind   browser                   r               check-new
macro  index,pager               T               "<pipe-message>notmuch_tag<enter>" "Manage notmuch tags"
bind   pager                     t               tag-message
macro  editor,index              Q               qqqq
bind   generic,index             $               last-entry
bind   generic,index             0               first-entry
bind   generic,pager             q               exit
macro  generic,pager             Q               qqqq
bind   generic,index,pager       /               search
bind   generic,index,pager       :               enter-command
bind   generic,index,pager       <F1>            search-opposite
bind   generic,index,pager       <F3>            search-next
bind   generic,index,pager       <PageDown>      next-page
bind   generic,index,pager       <PageUp>        previous-page

# vim: set filetype=muttrc nowrap :
